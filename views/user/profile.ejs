<%- include ../partials/header %>
<%- include ../partials/categories-navbar %>

<div class="container">
  <script>


    function myFunction0(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction1(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction2(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction3(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction4(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction5(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction6(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction7(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }
    
    function myFunction8(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }

    function myFunction9(text) {
      var copyText = document.getElementById("connectionstring0");
      navigator.clipboard.writeText(copyText.value);
      alert("Copied the connection string");

    }
    </script>
  <!-- Flash messages -->
      <div class="row mt-3 mb-3">
        <div class="col-md-8 m-auto">
          <% if (successMsg) { %>
          <div id="success" class="alert alert-success">
            <%= successMsg %>
          </div>
          <% } else { %>
          <div id="success" class="alert alert-success d-none"></div>
          <% } %>
        </div>
        <div class="col-md-8 m-auto">
          <% if (errorMsg) { %>
          <div id="error" class="alert alert-danger">
            <%= errorMsg %>
          </div>
          <% } else { %>
          <div id="error" class="alert alert-danger d-none"></div>
          <% } %>
        </div>
      </div>
  <!-- User's orders -->
  <div class="row mt-4">
    <div class="col-md-8 m-auto">
      <% if (orders != null && orders.length > 0) { %>
      <h3>My orders</h3>
      <% orders.forEach( order => { %>
      <div class="card m-3">
        <div class="card-body">
          <p class="card-text ">
            <ul class="list-group">
              <% order.cart.items.forEach( (item,index) => { %>
              <div >
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <%= item.title %> | <%= item.qty %> Units 
                  <span class="badge badge-info badge-pill"><%= item.price %></span>
                  <input type="hidden" value="<%= item?.v2ray?.url %>" id="connectionstring<%= index %>" />
                  <button onclick="myFunction<%= index %>()" class="btn btn-primary button-style">
                    Copy connection string
                  </button>
                </li>
           
              </div>
              <%}) %>
            </ul>
          </p>
        </div>
        <div class="card-footer">
          <strong>Total Price: $<%= order.cart.totalCost %></strong>
        </div>
      </div>
      <%}) %>
      <%} else { %>
        <h3 class="text-center" >You have not made any orders yet</h3>
      <%} %>
    </div>
  </div>
</div>

<%- include ../partials/footer %>
